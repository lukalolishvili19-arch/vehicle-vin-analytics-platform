# Production environment configuration

app_env: prod
log_level: WARNING

paths:
  data_root: data
  source_csv: data/raw/vehicle_search

postgres:
  host: localhost
  port: 5432
  database: vehicle_analytics
  user: vehicle_user

pipeline:
  batch_size: 10000
  quarantine_on_error: true
